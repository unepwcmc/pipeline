import arcpy


def buffer_to_rep_area(pt):
	# add colume to calculate radius
	# geodesic
	return buf_pt


def merge(input_list):
	# merge two polygons into one single

	return inputFC


def repair_geometry(inputFC):
	# ensures no geometry is left unrepaired
	# esri WILL modify the inputFC

	return inputFC

def dissolve_geometry(inputFC):
	# ensure no duplicates
	# massive performance increase required

	## tag PAME to enable later grouping
	## keep iso3 fields from the WDPA (not just the WVS iso3?)

	return dissolveFC

def main(pt, poly):
	# wrapper to create output feature class that is 

	return outputFC
